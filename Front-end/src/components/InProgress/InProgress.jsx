import "./InProgress.css";

/**
 * Render explanation paragraph of development status
 * @returns {JSX} Return of required JSX
 */

const InProgress = () => {
  return <p className="inProgress">🚧 Page en cours de Développement 🚧</p>;
};

export default InProgress;
